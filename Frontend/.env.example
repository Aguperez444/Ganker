# Copiar este archivo como `.env` (no se sube al repo) y ajustar si hace falta.
# Vite solo expone al navegador las variables que empiezan con VITE_.

# URL base de la API contra la que trabaja el frontend.
VITE_API_URL=http://127.0.0.1:8000
