// Se ejecuta antes de cada archivo de test (ver `test.setupFiles` en vite.config.js).
// Agrega los matchers de jest-dom: toBeInTheDocument(), toHaveTextContent(), etc.
import "@testing-library/jest-dom";
