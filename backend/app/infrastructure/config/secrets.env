#Json web token secret key
JWT_SECRET_KEY=Ganker
# Base de datos
# Opcional en local (por defecto usa sqlite:///.../db.db).
# Descomentar si se quiere probar PostgreSQL fuera de Docker:
# AÑADIR USUARIO Y CONTRASEÑA REAL PARA PROBAR FUERA DE DOCKER
# DATABASE_URL=postgresql+psycopg2://usuario:password@localhost:5432/nombre_db